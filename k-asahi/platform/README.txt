This directory contains Kernel Package build helpers.
These are executed during the construction of the OS InitRD,
and the only use case is to install a firmware blob into the
OS InitRD for the Pinebook Pro's display.
